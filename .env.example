# Backend WebSocket base URL (used by the frontend; `/api/game-meta` is fetched from this origin too)
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# Clerk — leave empty to run guest-only (auth UI hides, game still plays)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_USE_SVG_SPRITES=1